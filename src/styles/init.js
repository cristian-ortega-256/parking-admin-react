import normalizeStyles from './normalize'

export default () => {
  // Normalize styles.
  normalizeStyles()

  // Inject global styles with font-face declarations.
  //initFonts({ regular, semiBold, bold, black, italic })
}
